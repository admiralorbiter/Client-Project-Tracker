type Project {
    id: Int!
    status: String
    owner: String
    effort: Int
    due: String
    title: String!
}

type Query {
    about: String!
    projectList: [Project!]!
}
type Mutation {
    setAboutMessage(message: String!): String
}